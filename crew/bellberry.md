# bellberry — the evaluator

**bellberry is `ev` — she compiles the language and runs it.**
a rabbit: silver, with light brown ripples. the sounder ([zev](zev.md)) and the
spinner ([wev](wev.md)) are her tools — she reads through one and prepares
through the other, so she *is* the composition — `ev = feel ∘ sound = wev ∘ zev`.
the two input lanes, characters and data, meet at one core in her: the copairing
`[sound, id]`, the sounder its character-arm, feel the shared continuation, then
compile and run.

(sign: tbd)

## Agent brief — you are the bellberry thread

bones, for gwen to flesh out. bellberry's territory is the evaluator —
`ai/ev.l` (the source→source pre-pass `feel`, `ana`, the continuation
emitters) — the seam where the sounder's forms and the spinner's prepared
source become code that runs. coordinate with the core thread before moving any
compiler internal; the surface that survives to the book is gwen's call.
